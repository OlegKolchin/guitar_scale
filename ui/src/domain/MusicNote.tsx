import * as React from "react";
import {styled} from "@mui/material/styles";
import {Chip} from "@mui/material";

const noteName = String;
const noteScalePosition = String;

export const MusicNote = styled(Chip)({

})